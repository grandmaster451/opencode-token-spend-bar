- 2026-04-13: Locked ChatGPT Plus display semantics to usage-only by separating `shouldShowCost` from `formatCost`; callers must suppress currency entirely for `chatgpt-plus`.
- 2026-04-13: Updated invalid `@opentui/core` and `@opentui/solid` peer version ranges from `^1.0.0` to `^0.1.99` so `npm install` can complete and domain tests can be verified.
- 2026-04-13: Chose a namespaced monthly-ledger layout under `token-spend-bar:v1` with a global schema/month pointer plus per-month aggregate and processed-record keys so corruption and month rollovers can fall back to a clean rebuild instead of crashing.
- 2026-04-13: Implemented history scanning as a message-first pass with per-message `step-finish` aggregation; provider normalization stays in the domain layer and messages without supported providers or billable usage are dropped.
- 2026-04-13: Built the widget aggregation service on top of the KV ledger with fingerprint dedupe (`provider:timestamp:tokens`) and namespace-style history imports so rebuilds, live updates, and month rollover all share one view-model path without leaking storage structure into the UI.
- 2026-04-13: Kept the package entry backward-compatible by preserving the default export and adding named `TokenSpendBarPlugin`/`tui` exports, so OpenCode-style module loading and direct test imports both resolve through `src/index.tsx`.
- 2026-04-13: Triggered `api.renderer.requestRender()` after accepted live updates and unsubscribed the `message.updated` handler via `api.lifecycle.onDispose()` so prompt-right slot state refreshes immediately without leaking listeners across plugin restarts.
- 2026-04-13: Added a dedicated `test/fixtures/` harness plus `qa:tui-snapshot`/`qa:full` node scripts so unattended QA can refresh snapshots, run the full Vitest suite, build the package, and validate `npm pack --dry-run` without any provider credentials.
- 2026-04-13: Strengthened dedupe fingerprints from `provider:timestamp:tokens` to `provider:timestamp:tokens:costOrNull` so same-token events with different cost values are preserved instead of collapsing into one record.
- 2026-04-13: Standardized reusable test infra behind `test/helpers/mock-kv.ts` and `test/helpers/test-utils.ts` to keep fixture, service, integration, and component tests aligned on one KV double and one row/view-model helper surface.
